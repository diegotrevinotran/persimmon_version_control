# Persimmon
## Commands
### init
Creates a new repository in the current directory, which will be used to track and manage changes. If you rerun this
command in a directory that already contains a repository, it will do nothing and return an error message.